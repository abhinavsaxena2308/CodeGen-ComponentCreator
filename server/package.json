{
  "name": "codegen-backend",
  "version": "1.0.0",
  "description": "CodeGen Express backend (Gemini integration)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "health": "node -e \"require('http').get('http://localhost:5000/health', (res) => { console.log('Status Code:', res.statusCode); res.on('data', (chunk) => { console.log('Body:', chunk.toString()); }); });\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}